
> [!CAUTION]
> **ESTADO DE SINCRONIZACIÓN**: Shadow Mode (No Comiteado).
> El script `cerebro_v8.py` y sus mutaciones NO están vinculados al repositorio `freqtrade-bestia`. 
> El `git ignore` o la omisión voluntaria mantienen este desarrollo solo en el entorno de ejecución de AWS. 
> Evitar `git pull` o `git reset` en AWS que puedan sobreescribir el `config.json` mutado por el cerebro.
